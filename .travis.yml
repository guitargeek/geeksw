language: python
dist: xenial

os:
  - linux

python:
  - 3.7


addons:
  apt:
    packages:
      - python-setuptools
      - libhdf5-serial-dev
      - pandoc
      - python-pygments
      - python3-pygments
      - nodejs

script:
  - pip install torch
  - cd tests
  - sh download_test_data.sh
  - cd ..
  - python setup.py pytest
